import Chart from './Chart';

// This chart displays the distribution of movies by year ( Area Chart )
const MoviesByYearChart = ({ data }) => {

  const options = {};

  return <Chart options={options} />;
};

export default MoviesByYearChart;
