import Chart from "./Chart";

// This chart displays the distribution of movies by runtime and rating ( Scatter Chart )
const RuntimeRatingChart = ({data}) => {

    const options = {};

    return <Chart options={options} />

}

export default RuntimeRatingChart
