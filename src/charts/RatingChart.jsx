import Chart from "./Chart";

// This chart displays the distribution of movies by rating ( Column Chart )
function RatingChart({data}) {

  const options = {};

  return <Chart options={options} />
}

export default RatingChart
